package Range;

import static Range.Water.cola;
import static Range.Water.sprite;

public class Mail {
    public static <volume, brand> void main (String [] args) {
        cola, (volume)"20", new Water (brand;Object cola;
        fanta, (volume)"10", new Water(brand;Object fanta;
        sprite, (volume)"05", new Water (brand;Object sprite;
        };
    }

